{
  "name": "@vemjs/monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "VectoUI-driven next-generation modal text editor Monorepo",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "oxlint": "^1.72.0",
    "prettier": "^3.9.4",
    "typescript": "~6.0.2"
  },
  "workspaces": [
    "packages/*",
    "plugins/*"
  ]
}
