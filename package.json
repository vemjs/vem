{
  "name": "@vemjs/monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "VectoUI-driven next-generation modal text editor Monorepo",
  "type": "module",
  "scripts": {
    "dev:web": "bun --filter @vemjs/web-app dev",
    "build:web": "bun --filter @vemjs/web-app build",
    "format": "prettier --write .",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "oxlint": "^1.72.0",
    "prettier": "^3.9.4",
    "typescript": "~6.0.2"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "plugins/*"
  ]
}
